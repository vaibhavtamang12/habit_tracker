[build]
base = ""
command = "CI= npm --prefix client install && npm --prefix client run build"
publish = "client/build"

[functions]
directory = "functions"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200